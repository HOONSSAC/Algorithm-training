#include <stdio.h>
#include "Kruskal.h"
#include "Prim.h"

int main(void){
    srand(time(NULL));

    int n = 10; // 정점의 수
    int m = 11; // 간선의 수

    //KruskalMST(n, m);
    //PrimMST(n, m);

    return 0;
}